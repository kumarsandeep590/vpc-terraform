# vpc-terraform
It will create:

    1 VPC,
    1 public subnet,
    1 Internet Gateway,
    1 Security Group
    1 EC2 instance with Nginx webserver

After cloning the repo, just run the below commands
ssh-keygen -f mykey
terraform init
terraform plan
terraform apply

